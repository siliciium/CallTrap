<Window x:Class="PhoneSim.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhoneSim"
        mc:Ignorable="d"
        Title="Bluetooth PhoneSim" Height="670" Width="1270" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="DarkScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="#FF111111">
                            <Track Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#FF1D1D1D"
                                                CornerRadius="2"
                                                BorderThickness="0"
                                                BorderBrush="Transparent"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>


    <Grid Background="Black">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1" />
            <!-- colonne pour le splitter -->
            <ColumnDefinition Width="1.5*" />
        </Grid.ColumnDefinitions>

        <!-- Colonne gauche -->
        <Border Grid.Column="0" Background="#FFFFD60B" BorderThickness="0,0,0,0" BorderBrush="#FFFFD60B" Cursor="">
            <Grid Background="#FFFFD60B">
                <Button x:Name="bt_call" Content="📞CALL" HorizontalAlignment="Right" VerticalAlignment="Top" Width="93" Background="#FFC9AA10" Foreground="#FFFAFD00" Click="bt_call_Click" Height="35" BorderBrush="{x:Null}" FontFamily="Cascadia Mono" FontSize="16">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    CornerRadius="3"
                                    BorderThickness="1"
                                    Padding="5"
                                    Background="#FFC9AA10"
                                    BorderBrush="#FFFFD60B">
                                <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    RecognizesAccessKey="True"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#FFC9AA10"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="#FFFFD60B"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <RichTextBox x:Name="tb_debug" Foreground="White" Background="Black" FontFamily="Cascadia Mono" BorderBrush="#FF282828" SelectionBrush="#FF282828" Margin="0,35,0,0" VerticalScrollBarVisibility="Auto" IsReadOnly="True">
                    <RichTextBox.Template>
                        <ControlTemplate TargetType="RichTextBox">
                            <Border BorderBrush="{TemplateBinding BorderBrush}"
    BorderThickness="{TemplateBinding BorderThickness}"
    Background="{TemplateBinding Background}">

                                <ScrollViewer x:Name="PART_ContentHost"
              VerticalScrollBarVisibility="Visible"
              HorizontalScrollBarVisibility="Disabled">

                                    <ScrollViewer.Resources>
                                        <Style TargetType="ScrollBar"
               BasedOn="{StaticResource DarkScrollBar}" />
                                    </ScrollViewer.Resources>

                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </RichTextBox.Template>
                </RichTextBox>
                <Label Content="Phone number" HorizontalAlignment="Left" Margin="6,6,0,0" VerticalAlignment="Top" Width="97" RenderTransformOrigin="0.5,0.5" Foreground="#FFAE930E" FontFamily="Cascadia Mono">
                    <Label.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform Angle="0.844"/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Label.RenderTransform>
                </Label>
                <TextBox x:Name="tb_num" PreviewTextInput="PhoneTextBox_PreviewTextInput" DataObject.Pasting="PhoneTextBox_Pasting" HorizontalAlignment="Left" Margin="104,8,0,0" TextWrapping="Wrap" Text="+33933333333" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" VerticalAlignment="Top" Width="148" Foreground="#FFFCFF00" Background="#FFC9AA10" BorderBrush="#FFFFD60B" FontFamily="Cascadia Mono" FontSize="16"/>

            </Grid>
        </Border>

        <!-- GridSplitter vertical -->
        <GridSplitter Grid.Column="1"
                  Width="5"
                  BorderThickness="0"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  ResizeBehavior="PreviousAndNext"
                  ResizeDirection="Columns"
                  Background="#FFFFD60B" Cursor="SizeWE" />

        <!-- Colonne droite #FFFFD60B -->

        <Border Grid.Column="2" Background="#FFFFD60B" BorderThickness="0,0,0,0" BorderBrush="#FFFFD60B">
            <Grid Background="#FFFFD60B">
                <Label Content="Phonebook" HorizontalAlignment="Center" HorizontalContentAlignment="Center" Margin="6,6,0,0" VerticalAlignment="Top" Width="97" RenderTransformOrigin="0.5,0.5" Foreground="#FFAE930E" FontFamily="Cascadia Mono" Padding="5,5,5,5">
                    <Label.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform Angle="0.844"/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Label.RenderTransform>
                </Label>
                <RichTextBox x:Name="tb_pbap" Foreground="White" Background="Black" FontFamily="Cascadia Mono" BorderBrush="#FF282828" SelectionBrush="#FF282828" Margin="0,35,0,0" VerticalScrollBarVisibility="Auto" IsReadOnly="True">
                    <RichTextBox.Template>
                        <ControlTemplate TargetType="RichTextBox">
                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Background="{TemplateBinding Background}">

                                <ScrollViewer x:Name="PART_ContentHost"
                              VerticalScrollBarVisibility="Visible"
                              HorizontalScrollBarVisibility="Disabled">

                                    <ScrollViewer.Resources>
                                        <Style TargetType="ScrollBar"
                               BasedOn="{StaticResource DarkScrollBar}" />
                                    </ScrollViewer.Resources>

                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </RichTextBox.Template>
                </RichTextBox>
            </Grid>
        </Border>
    </Grid>
</Window>

